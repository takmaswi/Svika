# V2 — chore: video-assets folder for hackathon submission

> Paste this whole document into Claude Code as one job. This is a file-organization task only. No code changes, no tests, no migrations. Just `mkdir`, `cp`, and writing four small markdown / text files.

## Why

We are submitting a 3-minute video for the GDG Harare Build with AI hackathon by 2026-04-30 23:59 CAT. The video will be generated by Google AI tools (NotebookLM Cinematic Video Overview, with Google Flow / Veo as backup). Those tools ingest a folder of source materials and produce video. The folder needs to be:

1. In the repo root so it is easy to find.
2. Self-explanatory: any AI tool that opens it should understand what Svika is and what the video has to communicate, without external context.
3. Copies, not moves. Originals stay where they are.

## Deliverable

A new folder `video-assets/` in the repo root, populated with brand artwork copied from existing locations, V1 rehearsal screenshots, and four context documents (`README.md`, `CONTEXT.md`, `SCRIPT.md`, `palette.txt`, `typography.txt`).

## Steps

### Step 1: create the directory tree

```bash
mkdir -p video-assets/references/brand
mkdir -p video-assets/references/mockups
mkdir -p video-assets/references/context
mkdir -p video-assets/audio
mkdir -p video-assets/generated
mkdir -p video-assets/final
```

### Step 2: copy brand artwork into `video-assets/references/brand/`

Copy (not move) the V1 brand SVG outputs and the v2 brand HTML system:

```bash
cp public/brand/v2/logo.svg            video-assets/references/brand/logo.svg
cp public/brand/v2/wordmark.svg        video-assets/references/brand/wordmark.svg
cp "public/branding/v2/Svika Brand.html"     "video-assets/references/brand/svika-brand.html"
cp "public/branding/v2/Svika B1 System.html" "video-assets/references/brand/svika-b1-system.html"
```

If `public/brand/v2/logo.svg` or `wordmark.svg` does not exist (they were created during V1), check `public/branding/` for any wordmark or hero asset and copy what is there. Do not block the rest of this brief on a missing file. Note the gap in the run report.

### Step 3: copy V1 rehearsal mockups into `video-assets/references/mockups/`

Rename them as you copy so they sort by scene order, not just by index:

```bash
cp scripts/phase-V1-rehearsal-1.png video-assets/references/mockups/01-landing.png
cp scripts/phase-V1-rehearsal-2.png video-assets/references/mockups/02-suburb-picker.png
cp scripts/phase-V1-rehearsal-3.png video-assets/references/mockups/03-idle-map.png
cp scripts/phase-V1-rehearsal-4.png video-assets/references/mockups/04-trip-preview.png
cp scripts/phase-V1-rehearsal-5.png video-assets/references/mockups/05-wallet.png
cp scripts/phase-V1-rehearsal-6.png video-assets/references/mockups/06-hwindi-keypad.png
cp scripts/phase-V1-rehearsal-7.png video-assets/references/mockups/07-fleet-dashboard.png
cp scripts/phase-V1-rehearsal-8.png video-assets/references/mockups/08-wa-companion.png
```

### Step 4: copy reference photos into `video-assets/references/context/`

```bash
cp "public/branding/v2/uploads/kombi side.png"                    "video-assets/references/context/kombi-side-source.png"
cp "public/branding/v2/uploads/brown-van-template-free-vector.jpg" "video-assets/references/context/kombi-template.jpg"
```

If either upload is missing or has a different filename, list what is in `public/branding/v2/uploads/` and copy whatever looks usable. Note in the run report.

### Step 5: write `video-assets/README.md`

Exact content:

```
# video-assets/

Source materials for the Svika hackathon submission video.

## Structure

- references/
  - brand/      Logo, wordmark, palette, typography, brand HTML system
  - mockups/    V1 app screenshots, 8 frames in scene order
  - context/    Reference photos and source artwork
- audio/        Voiceover and music (added during production)
- generated/    AI-tool output clips
- final/        Final rendered MP4

## How to use

Any AI video tool that ingests text and images can be pointed at this folder. The CONTEXT.md file at the top is the single source of truth for what Svika is and what the video has to communicate. SCRIPT.md is the locked voice script.

## Originals

Files here are copies. Originals stay at:
- public/brand/v2/                    (logo, wordmark)
- public/branding/v2/                 (brand HTML system)
- public/branding/v2/uploads/         (reference photos)
- scripts/phase-V1-rehearsal-*.png    (V1 rehearsal mockups)
```

### Step 6: write `video-assets/CONTEXT.md`

Exact content:

```
# Svika, context for AI video tooling

## What Svika is

Svika is a digital ticketing and trip-planning system for Harare's informal kombi (minibus) network. It is a hackathon submission for the GDG Harare Build with AI 2026 hackathon.

## The pitch in one sentence

In Zimbabwe, riding a kombi means asking a stranger which one to take, waiting blind for it to arrive, and fighting over change when it does. Svika fixes all three.

## Who it serves

A working professional in Harare who rides kombis several times a week. They speak Shona and English. They have a smartphone with intermittent data. They want to know which kombi to take, when it will arrive, and what they will pay, without arguing.

## What the video has to do

Three minutes total. Punchy and confident tone. Linear-style restraint, not Pixar excess.

- 0:00 to 0:08: Three quick text frames naming the three pains.
- 0:08 to 0:14: Brand reveal, the wordmark on Bone background.
- 0:14 to 1:45: Animated walkthrough of the rider experience: location-first onboarding, Shona trip planning, fare-cleared toast, fleet impact disclosure.
- 1:45 to 2:00: Fleet owner sees the bilingual revenue audit produced on-device by Gemma.
- 2:00 to 3:00: Static walkthrough of the live app surfaces (real screenshots, narrated).
- Last 5 seconds: Roadmap close, the URL, the GitHub link.

## Brand language

Colors:
- Forest   #1F4D2E   primary CTA, primary ink, primary surface
- Pine     #0E3A1E   pressed states
- Char     #0E1A12   body copy
- Bone     #FFFCEF   page background, light surface
- Linen    #E9E2C8   muted surface
- Signal   #E84C30   accent, key verbs, live indicator
- Moss     #4D5C44   muted text

Typography:
- Display: DM Sans 700, used for headlines and the wordmark voice
- Body:    IBM Plex Sans, used for paragraphs and UI text
- Mono:    IBM Plex Mono, used for codes, fares, timestamps

All three typefaces are SIL OFL 1.1 licensed.

## Voice direction

The narrator is the founder, a Zimbabwean working professional. Calm, confident, plain English with the occasional Shona phrase. Not corporate, not breathless. Reads like someone who actually rides kombis and built the thing because the friction bugged them every day.

## Google AI tools used (the 20 percent Google pillar)

- Gemini 2.5 Flash: live Shona and English trip understanding inside the app.
- Gemma 4 E2B running on-device via Ollama: bilingual revenue audit narrative on the fleet dashboard.
- Gemini Deep Research: planning research while drafting the product requirements document.
- Custom Gemini Gem: context container during PRD authoring.
- Google AI Studio + Google Maps: trip route data extraction (rendered via Mapbox in product).
- Google Antigravity: agentic IDE used to write and refactor the TypeScript codebase.
- Google NotebookLM Cinematic Video Overview (Gemini + Imagen + Veo): this submission video.

## Real moments to land in the video

1. Speaking Shona to get a trip plan. The app understands the language. The plan includes a walking transfer locals know but visitors never figure out.
2. The fare-cleared toast. The conductor enters the rider's three-digit code on a separate screen; the rider's phone receives the confirmation in real time.
3. The fleet impact card. The rider arrives and a quiet line tells them where their fare landed. They tap, and the bilingual Gemma audit narrative appears on the fleet dashboard, in Shona for home and English for the bank.

## What NOT to show

- Real EcoCash, Paynow, or any money movement (top-ups are mocked).
- Real WhatsApp Business or USSD aggregator (those surfaces are mocked or roadmap).
- Live ride simulation. The on-screen kombi animation has a known bug we did not fix in the available time. The video tells that part of the story through animation, not screen capture.
- "Continue as Takunda" CTA on the landing page. Removed in V1. The landing now uses location-first onboarding, then a suburb picker as fallback.

## Output format

3-minute MP4, 1080p preferred, 60 fps if possible, AAC audio. Uploaded as YouTube unlisted for the submission form.
```

### Step 7: write `video-assets/SCRIPT.md`

Exact content (placeholder, will be overwritten when the script is locked):

```
# Voice script for Svika submission video

## Locked so far

- Tone: punchy and confident, Linear style
- North star: "In Zimbabwe, riding a kombi means asking a stranger which one to take, waiting blind for it to arrive, and fighting over change when it does. Svika fixes all three."

## To lock

- Cold open card content (the three pain frames)
- Brand bumper card
- Three real-moment label cards
- Roadmap close card
- Full voiceover script, around 450 words for 3 minutes

The full script will be written into this file once locked.
```

### Step 8: write `video-assets/references/brand/palette.txt`

Exact content:

```
Forest    #1F4D2E    primary CTA, primary ink, primary surface
Pine      #0E3A1E    pressed states
Char      #0E1A12    body copy
Bone      #FFFCEF    page background, light surface
Linen     #E9E2C8    muted surface (cards, sheet bg)
Signal    #E84C30    accent, live indicator, key verbs
Moss      #4D5C44    muted text
Live dot  #16A34A    pulsing live indicator (legacy, may remap to Signal)
```

### Step 9: write `video-assets/references/brand/typography.txt`

Exact content:

```
Display: DM Sans 700 (headlines, wordmark voice)
Body:    IBM Plex Sans (paragraphs, UI text, weights 400/500/600/700)
Mono:    IBM Plex Mono (codes, fares, timestamps)

All three are SIL OFL 1.1 licensed, free for commercial use.
Hosted via Google Fonts; loaded in app/layout.tsx via next/font/google.
```

### Step 10: add to repo `.gitignore`

The `audio/`, `generated/`, and `final/` subfolders will hold large binary outputs. Append to `.gitignore`:

```
# video-assets generated outputs
video-assets/audio/*.wav
video-assets/audio/*.mp3
video-assets/audio/*.m4a
video-assets/generated/
video-assets/final/
```

Keep the folders themselves tracked by adding empty `.gitkeep` files in each:

```bash
touch video-assets/audio/.gitkeep
touch video-assets/generated/.gitkeep
touch video-assets/final/.gitkeep
```

Whitelist the .gitkeep files via `!` rules in .gitignore if the patterns above would catch them.

### Step 11: commit

```bash
git add \
  video-assets/ \
  .gitignore \
  docs/rebuild/V2-video-assets-brief.md

git commit -m "chore: video-assets folder for hackathon submission video"
git push origin main
```

## Run report

After all steps, surface a tree of `video-assets/` (using `tree` or recursive `ls`) so we can confirm every file landed. Note any missing source files that were skipped.

## Stop conditions

- A source file the brief expects is missing and there is no obvious substitute. Note the gap and continue with the rest.
- Any of the markdown text content above contains a stray em or en dash that the file write would carry forward. The brief uses commas, colons, and parentheses only. If a paste introduces a dash, sanitize.
- A `cp` fails due to a path with spaces and quoting issues. Try double-quoting both source and destination, or use single-quotes.

End of brief.
